coverage:
  status:
    project:
      default:
        target: 90%
        threshold: 3%
    patch:
      default:
        target: 90%
        threshold: 3%

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: false
